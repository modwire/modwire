name: Issue metadata

on:
  issues:
    types:
      - opened
      - edited
      - typed
      - untyped
      - field_added
      - field_removed
      - labeled
      - unlabeled

jobs:
  validate:
    uses: ./.github/workflows/issue-metadata.yml
